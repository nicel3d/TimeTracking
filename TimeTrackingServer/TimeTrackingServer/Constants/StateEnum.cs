namespace TimeTrackingServer.Constants
{
    public enum StateEnum
    {
        Allowed,
        Forbidden,
        Neutral
    }
    public enum ModeEnum
    {
        Contains,
        Exactly,
        DoesNotContain
    }
}
